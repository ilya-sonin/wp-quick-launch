# Изменения в проекте

## Что изменилось

### ✅ Добавлено
- `wordpress-6.9-ru_RU.zip` (34MB) - архив WordPress для автоматической распаковки
- `start.sh` - интерактивный скрипт запуска с автонастройкой
- `stop.sh` - скрипт остановки и очистки
- `README.md` - полная документация
- `QUICK_START.md` - быстрая инструкция
- `wordpress-setup.sh` - вспомогательный скрипт для создания архива

### 🔄 Изменено
- `docker-compose.yml` - упрощен, все через .env переменные
- `upload.ini` - настройки PHP (256MB лимит)
- `.gitignore` - обновлен для новой структуры

### ❌ Удалено из git
- `.env` - теперь генерируется автоматически (не коммитится)
- `wordpress/` - вся папка не коммитится (создается из архива)
- `wp-content/` из корня - перенесена в `wordpress/`
- Старые скрипты из `scripts/`

## Новая структура

```
wp-my-project/
├── docker-compose.yml          # Docker конфигурация
├── upload.ini                 # PHP настройки
├── start.sh                   # 🚀 Главный скрипт
├── stop.sh                    # Остановка
├── README.md                  # Документация
├── QUICK_START.md            # Быстрый старт
├── wordpress-6.9-ru_RU.zip   # WordPress архив (коммитится!)
└── wordpress/                # НЕ коммитится (создается автоматически)
    ├── wp-content/
    ├── wp-admin/
    └── wp-includes/
```

## Как использовать

### Для нового разработчика:
```bash
git clone <repo>
cd wp-my-project
./start.sh
```

Всё! WordPress распакуется автоматически, настроится и запустится.

### Для обновления:
```bash
git pull
./start.sh
```

Если были изменения в архиве - скрипт распакует его заново.

